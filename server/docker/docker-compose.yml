version: '3.8'

services:

  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6380:6379"

  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27018:27017"

